namespace Server.Infrastructure.Synchronization.Groups
{
    /// <summary>
    /// A group of related values that can be stored that need to be sync'd. 
    /// </summary>
    public abstract class SyncGroup<T>
    {
    
    }
}
