/*
	A game system is used to interact and manipulate objects in the game world.
*/

module.exports = function() {

	GameSystem = Class.extend({

		init: function(game) {
			this.game = game;
		},

		/*
			Updates the system and allows it to perform operations on the entities it cares about.	
		*/
		update: function(time)
		{	
		
		}


	});

}() l